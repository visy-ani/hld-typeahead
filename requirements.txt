# Runtime dependencies for the Search Typeahead System.
# Python 3.11+ recommended (developed and tested on 3.14).
fastapi>=0.115          # web framework (pulls in pydantic + starlette)
uvicorn[standard]>=0.30 # ASGI server (uvloop/httptools for speed)
httpx>=0.27             # async HTTP client used by the benchmark harness

# Dev / test only:
pytest>=8.0
